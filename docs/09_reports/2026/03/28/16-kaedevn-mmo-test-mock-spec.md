---
title: kaedevn テキスト型MMO テスト仕様書・モック仕様書
type: spec
project: colyseus + kaedevn
version: 0.17.x
date: 2026-03-28
author: Claude Opus 4.6
---

# kaedevn テキスト型MMO テスト仕様書・モック仕様書

> **Repository:** colyseus + kaedevn-monorepo
> **Generated by:** Claude Opus 4.6 | **Date:** 2026-03-28
> **Scope:** MMO 統合テスト全体

## 次回セッション向けコンテキスト

この文書は、kaedevn テキスト型 MMO の開発を次回セッションから開始するために必要な情報をすべて含む。Colyseus の Room 実装 + kaedevn エンジン統合のテストを書くために必要なモック、テストケース、データ構造を網羅する。

---

## 1. 両リポジトリの接続ポイント

### kaedevn 側（既存）

| コンポーネント | ファイル | 役割 |
|-------------|---------|------|
| JWT 認証 | `apps/hono/src/lib/auth.ts` | `generateToken(userId, expiresIn?, claims?)` / `verifyToken(token)` |
| 認証ミドルウェア | `apps/hono/src/middleware/auth.ts` | Bearer トークン検証、role/status チェック |
| User DB | `apps/hono/prisma/schema.prisma` | User テーブル（id, email, role, status） |
| セーブデータ | CloudSave テーブル | `{ save_schema_version, work_id, scenario_id, node_id, vars, read, timestamp }` |
| 図形キャラ | `packages/web/src/chara-editor.ts` | drawCharacter, EXPRESSIONS(18種), POSES(16種), PRESETS |
| インラインタグ | `packages/web/src/chara-editor.ts` | parseInlineText, buildTimeline, executeDirective |
| テキスト表示 | `packages/web/src/renderer/TextWindow.ts` | PixiJS Text + Graphics、文字送り（charAccum） |
| IOpHandler | `packages/core/src/engine/IOpHandler.ts` | textAppend, waitClick, chSet, bgmPlay 等 |

### Colyseus 側（既存）

| コンポーネント | ファイル | 役割 |
|-------------|---------|------|
| Room 基底 | `packages/core/src/Room.ts` | onCreate, onAuth, onJoin, onLeave, onDispose |
| MatchMaker | `packages/core/src/MatchMaker.ts` | joinOrCreate, create, join, filterBy |
| Transport | `packages/core/src/Transport.ts` | WebSocket 抽象化 |
| Presence | `packages/presence/redis-presence/` | pub/sub、IPC |
| Driver | `packages/drivers/redis-driver/` | Room キャッシュ |
| テスト基盤 | `packages/testing/` | ColyseusTestServer |
| SDK | `packages/sdk/src/Client.ts` | joinOrCreate, send, onMessage, onStateChange |

### 新規実装が必要なコンポーネント

| コンポーネント | 配置先 | 役割 |
|-------------|--------|------|
| WorldRoom | colyseus リポジトリ | ゾーン管理、プレイヤー同期 |
| ChatRoom | colyseus リポジトリ | グローバル/ゾーン/ウィスパーチャット |
| BattleRoom | colyseus リポジトリ | ターン制戦闘 |
| TradeRoom | colyseus リポジトリ | アイテム取引 |
| PlayerState | colyseus リポジトリ | @colyseus/schema で定義するプレイヤー状態 |
| WorldState | colyseus リポジトリ | ゾーンの状態（NPC、オブジェクト） |
| KaedevnAuthAdapter | colyseus リポジトリ | kaedevn JWT → Colyseus onAuth 変換 |
| PlayerPersistence | colyseus リポジトリ | PostgreSQL 永続化レイヤー |

---

## 2. データ構造定義（@colyseus/schema）

### PlayerState

```typescript
import { Schema, type, MapSchema } from "@colyseus/schema";

class PlayerState extends Schema {
  @type("string") sessionId: string = "";
  @type("string") name: string = "";
  @type("string") gender: "female" | "male" = "female";
  @type("string") preset: string = "hanako";     // 図形キャラプリセット
  @type("string") expression: string = "normal";  // 現在の表情
  @type("string") pose: string = "standing";       // 現在のポーズ
  @type("string") lipMode: string = "off";         // リップシンクモード
  @type("number") x: number = 0;                   // ゾーン内位置
  @type("number") y: number = 0;
  @type("string") status: string = "idle";         // idle | talking | battle
}
```

### WorldState

```typescript
class NPCState extends Schema {
  @type("string") id: string = "";
  @type("string") name: string = "";
  @type("string") expression: string = "normal";
  @type("string") pose: string = "standing";
  @type("number") x: number = 0;
  @type("number") y: number = 0;
  @type("string") dialogue: string = "";           // 現在の表示テキスト
}

class WorldState extends Schema {
  @type("string") zoneId: string = "";
  @type("string") zoneName: string = "";
  @type({ map: PlayerState }) players = new MapSchema<PlayerState>();
  @type({ map: NPCState }) npcs = new MapSchema<NPCState>();
  @type("number") time: number = 0;                // ゲーム内時間
  @type("string") weather: string = "clear";
}
```

### ChatMessage（非 Schema — メッセージ型）

```typescript
interface ChatMessage {
  sender: string;       // プレイヤー名
  text: string;         // インラインタグ含む可能性あり
  channel: "global" | "zone" | "party" | "whisper";
  targetId?: string;    // whisper の場合
  timestamp: number;
}
```

### BattleState

```typescript
class BattlerState extends Schema {
  @type("string") id: string = "";
  @type("string") name: string = "";
  @type("number") hp: number = 100;
  @type("number") maxHp: number = 100;
  @type("number") mp: number = 50;
  @type("string") status: string = "alive";        // alive | dead | fled
  @type("boolean") isPlayer: boolean = true;
}

class BattleState extends Schema {
  @type("string") phase: string = "waiting";       // waiting | selecting | executing | result
  @type("number") turn: number = 0;
  @type({ map: BattlerState }) battlers = new MapSchema<BattlerState>();
  @type("string") currentActorId: string = "";
  @type("string") log: string = "";                // 戦闘ログ（インラインタグ含む）
}
```

---

## 3. モック仕様

### 3.1 kaedevn JWT モック

kaedevn の JWT を Colyseus テスト内で再現する。

```typescript
// test/mocks/kaedevn-auth.ts
import jwt from "jsonwebtoken";

const TEST_JWT_SECRET = "test-secret-for-colyseus-mmo";

interface KaedevnTokenPayload {
  userId: string;
  role: "user" | "admin" | "guest";
  status: "active" | "suspended";
}

export function createTestToken(payload: Partial<KaedevnTokenPayload> = {}): string {
  return jwt.sign({
    userId: payload.userId || "test-user-001",
    role: payload.role || "user",
    status: payload.status || "active",
  }, TEST_JWT_SECRET, { expiresIn: "1h" });
}

export function createExpiredToken(): string {
  return jwt.sign({ userId: "expired-user", role: "user", status: "active" }, TEST_JWT_SECRET, { expiresIn: "0s" });
}

export function createGuestToken(): string {
  return jwt.sign({ userId: "guest-001", role: "guest", status: "active" }, TEST_JWT_SECRET, { expiresIn: "72h" });
}

export function createSuspendedToken(): string {
  return jwt.sign({ userId: "banned-user", role: "user", status: "suspended" }, TEST_JWT_SECRET, { expiresIn: "1h" });
}

export function verifyTestToken(token: string): KaedevnTokenPayload | null {
  try {
    return jwt.verify(token, TEST_JWT_SECRET) as KaedevnTokenPayload;
  } catch {
    return null;
  }
}
```

### 3.2 PostgreSQL モック（プレイヤー永続化）

テスト時は InMemory ストアで代替。本番は Prisma + PostgreSQL。

```typescript
// test/mocks/player-db.ts

interface PlayerData {
  userId: string;
  name: string;
  gender: "female" | "male";
  preset: string;
  zoneId: string;
  x: number;
  y: number;
  hp: number;
  mp: number;
  level: number;
  exp: number;
  gold: number;
  inventory: InventoryItem[];
  questProgress: Record<string, QuestState>;
  lastLogin: number;
}

interface InventoryItem {
  itemId: string;
  name: string;
  quantity: number;
  type: "consumable" | "equipment" | "key";
}

interface QuestState {
  questId: string;
  status: "active" | "completed" | "failed";
  progress: Record<string, number>;
}

export class InMemoryPlayerDB {
  private store = new Map<string, PlayerData>();

  async findByUserId(userId: string): Promise<PlayerData | null> {
    return this.store.get(userId) || null;
  }

  async save(data: PlayerData): Promise<void> {
    this.store.set(data.userId, { ...data });
  }

  async delete(userId: string): Promise<void> {
    this.store.delete(userId);
  }

  // テスト用: 初期データ投入
  seed(players: PlayerData[]): void {
    players.forEach(p => this.store.set(p.userId, p));
  }

  // テスト用: 全データクリア
  clear(): void {
    this.store.clear();
  }
}

// デフォルトのプレイヤーデータ（新規作成時）
export function defaultPlayerData(userId: string, name: string): PlayerData {
  return {
    userId,
    name,
    gender: "female",
    preset: "hanako",
    zoneId: "zone-001-village",
    x: 480,
    y: 360,
    hp: 100,
    mp: 50,
    level: 1,
    exp: 0,
    gold: 100,
    inventory: [
      { itemId: "potion-001", name: "回復薬", quantity: 3, type: "consumable" },
    ],
    questProgress: {},
    lastLogin: Date.now(),
  };
}
```

### 3.3 ゾーンマップモック

```typescript
// test/mocks/zone-map.ts

interface ZoneDefinition {
  id: string;
  name: string;
  description: string;
  maxPlayers: number;
  adjacentZones: { direction: string; zoneId: string }[];
  npcs: NPCDefinition[];
  bgAssetId?: string;    // kaedevn のアセットID
}

interface NPCDefinition {
  id: string;
  name: string;
  expression: string;    // 初期表情
  pose: string;          // 初期ポーズ
  x: number;
  y: number;
  dialogue: string[];    // 固定セリフ（インラインタグ含む）
}

export const TEST_ZONES: ZoneDefinition[] = [
  {
    id: "zone-001-village",
    name: "はじまりの村",
    description: "穏やかな村。冒険者たちの拠点。",
    maxPlayers: 50,
    adjacentZones: [
      { direction: "north", zoneId: "zone-002-forest" },
      { direction: "east", zoneId: "zone-003-market" },
    ],
    npcs: [
      {
        id: "npc-elder",
        name: "長老ヨハン",
        expression: "normal",
        pose: "standing",
        x: 400,
        y: 300,
        dialogue: [
          "[e:smile]ようこそ、旅人よ。[click]この村は平和じゃが...[e:serious]北の森には気をつけるのじゃ。",
          "[e:sad]最近、森から魔物が出るようになってのう...[click][e:smile]お主なら大丈夫じゃろう！",
        ],
      },
      {
        id: "npc-merchant",
        name: "商人マリア",
        expression: "smile",
        pose: "relaxed",
        x: 600,
        y: 350,
        dialogue: [
          "[e:smile][p:wave]いらっしゃい！[click]何かお探しかしら？",
        ],
      },
    ],
  },
  {
    id: "zone-002-forest",
    name: "霧の森",
    description: "薄暗い森。魔物が出没する。",
    maxPlayers: 30,
    adjacentZones: [
      { direction: "south", zoneId: "zone-001-village" },
      { direction: "north", zoneId: "zone-004-dungeon" },
    ],
    npcs: [],
  },
  {
    id: "zone-003-market",
    name: "交易広場",
    description: "各地から商人が集まる活気ある広場。",
    maxPlayers: 100,
    adjacentZones: [
      { direction: "west", zoneId: "zone-001-village" },
    ],
    npcs: [
      {
        id: "npc-trader",
        name: "旅商人ロイド",
        expression: "grin",
        pose: "cool",
        x: 500,
        y: 400,
        dialogue: [
          "[e:grin]よう！珍しいもんが入ったぜ。[click][e:wink]今なら特別価格だ。",
        ],
      },
    ],
  },
];

export function getZone(zoneId: string): ZoneDefinition | undefined {
  return TEST_ZONES.find(z => z.id === zoneId);
}

export function getAdjacentZone(currentZoneId: string, direction: string): string | undefined {
  const zone = getZone(currentZoneId);
  return zone?.adjacentZones.find(a => a.direction === direction)?.zoneId;
}
```

### 3.4 インラインタグパーサーモック

kaedevn の chara-editor.ts から抽出した純粋関数。テスト内で使用。

```typescript
// test/mocks/inline-tags.ts

type DirectiveType = "expr" | "pose" | "lip" | "cam" | "wait";

interface Directive {
  type: DirectiveType;
  value: string;
  param?: string;
}

interface TextSegment { kind: "text"; text: string; }
interface DirectiveSegment { kind: "directive"; directive: Directive; }
type Segment = TextSegment | DirectiveSegment;

function parseTag(content: string): Directive | null {
  if (content === "t") return { type: "lip", value: "talk" };
  if (content === "w") return { type: "lip", value: "whisper" };
  if (content === "s") return { type: "lip", value: "shout" };
  if (content === "click") return { type: "wait", value: "click" };
  if (content.startsWith("e:")) return { type: "expr", value: content.slice(2) };
  if (content.startsWith("p:")) return { type: "pose", value: content.slice(2) };
  if (content.startsWith("cam:")) {
    const parts = content.slice(4).split(":");
    return { type: "cam", value: parts[0], param: parts[1] };
  }
  if (content.startsWith("wait:")) return { type: "wait", value: content.slice(5) };
  return null;
}

export function parseInlineText(raw: string): Segment[] {
  const segments: Segment[] = [];
  let textBuf = "";
  let i = 0;
  while (i < raw.length) {
    if (raw[i] === "[") {
      const end = raw.indexOf("]", i + 1);
      if (end !== -1) {
        const directive = parseTag(raw.slice(i + 1, end));
        if (directive) {
          if (textBuf) { segments.push({ kind: "text", text: textBuf }); textBuf = ""; }
          segments.push({ kind: "directive", directive });
          i = end + 1;
          continue;
        }
      }
    }
    textBuf += raw[i];
    i++;
  }
  if (textBuf) segments.push({ kind: "text", text: textBuf });
  return segments;
}

export function stripTags(raw: string): string {
  return parseInlineText(raw)
    .filter(s => s.kind === "text")
    .map(s => (s as TextSegment).text)
    .join("");
}

export function extractDirectives(raw: string): Directive[] {
  return parseInlineText(raw)
    .filter(s => s.kind === "directive")
    .map(s => (s as DirectiveSegment).directive);
}
```

---

## 4. テストケース仕様

### 4.1 WorldRoom テスト

```
ファイル: test/WorldRoom.test.ts
フレームワーク: Mocha（Colyseus 標準）
```

#### 認証

| # | テストケース | 期待結果 |
|---|------------|---------|
| W-AUTH-01 | 有効な kaedevn JWT でジョイン | onAuth 通過、PlayerState 追加 |
| W-AUTH-02 | 期限切れ JWT でジョイン | AUTH_FAILED エラー、接続拒否 |
| W-AUTH-03 | suspended ユーザーでジョイン | AUTH_FAILED エラー |
| W-AUTH-04 | guest ユーザーでジョイン | 成功（制限付き） |
| W-AUTH-05 | トークンなしでジョイン | AUTH_FAILED エラー |

#### ゾーン参加・離脱

| # | テストケース | 期待結果 |
|---|------------|---------|
| W-JOIN-01 | プレイヤーが WorldRoom にジョイン | state.players にセッションID で追加 |
| W-JOIN-02 | プレイヤー情報がDBからロードされる | name, gender, preset, x, y が反映 |
| W-JOIN-03 | 新規プレイヤー（DB にデータなし） | defaultPlayerData で初期化 |
| W-JOIN-04 | maxClients(50) 到達後のジョイン | エラー返却（Room full） |
| W-LEAVE-01 | プレイヤーが離脱 | state.players から削除 |
| W-LEAVE-02 | 離脱時にDBへ自動保存 | PlayerData の x, y, hp 等が保存される |
| W-LEAVE-03 | 異常切断（onDrop） | 再接続猶予あり、タイムアウト後に onLeave |

#### ゾーン移動

| # | テストケース | 期待結果 |
|---|------------|---------|
| W-MOVE-01 | "move" メッセージ（north） | zone_change メッセージ送信、隣接ゾーンID含む |
| W-MOVE-02 | 存在しない方向に移動 | エラーメッセージ送信 |
| W-MOVE-03 | 移動先ゾーンが maxClients | エラーメッセージ（満員） |

#### NPC インタラクション

| # | テストケース | 期待結果 |
|---|------------|---------|
| W-NPC-01 | NPC に話しかける（"interact"） | NPC の dialogue テキスト送信 |
| W-NPC-02 | dialogue にインラインタグ含む | タグ付きテキストがそのまま送信される |
| W-NPC-03 | 存在しない NPC ID | エラーメッセージ |

#### プレイヤー同期

| # | テストケース | 期待結果 |
|---|------------|---------|
| W-SYNC-01 | プレイヤーA の表情変更 | 全クライアントに state パッチ配信 |
| W-SYNC-02 | プレイヤーA のポーズ変更 | 同上 |
| W-SYNC-03 | 同一ゾーンの他プレイヤーが見える | state.players に全員含まれる |

#### 永続化

| # | テストケース | 期待結果 |
|---|------------|---------|
| W-PERSIST-01 | Room dispose 時にゾーン状態保存 | DB に NPC 状態、時間が保存される |
| W-PERSIST-02 | Room 再作成時にゾーン状態復元 | 前回の状態から再開 |

### 4.2 ChatRoom テスト

```
ファイル: test/ChatRoom.test.ts
```

| # | テストケース | 期待結果 |
|---|------------|---------|
| C-GLOBAL-01 | グローバルチャット送信 | 全クライアントに broadcast |
| C-GLOBAL-02 | インラインタグ付きチャット | タグが保持されたまま配信 |
| C-ZONE-01 | ゾーンチャット送信 | 同一ゾーンのクライアントのみ受信 |
| C-WHISPER-01 | ウィスパー送信 | 指定ターゲットのみ受信 |
| C-WHISPER-02 | 存在しないターゲット | エラーメッセージ返却 |
| C-RATE-01 | メッセージ連投（レート制限） | 超過分はドロップ |
| C-FORMAT-01 | 空メッセージ送信 | 無視される |
| C-FORMAT-02 | 長すぎるメッセージ（500文字超） | 切り詰めまたは拒否 |

### 4.3 BattleRoom テスト

```
ファイル: test/BattleRoom.test.ts
```

#### ライフサイクル

| # | テストケース | 期待結果 |
|---|------------|---------|
| B-CREATE-01 | BattleRoom 作成（PvE） | phase=waiting、敵 NPCBattler 配置 |
| B-CREATE-02 | BattleRoom 作成（PvP） | phase=waiting、全プレイヤー参加待ち |
| B-JOIN-01 | 全プレイヤー参加完了 | phase → selecting に遷移 |
| B-END-01 | 全敵 HP 0 | phase → result、勝利判定 |
| B-END-02 | 全味方 HP 0 | phase → result、敗北判定 |
| B-END-03 | 逃走成功 | phase → result、flee 判定 |

#### ターン制戦闘

| # | テストケース | 期待結果 |
|---|------------|---------|
| B-TURN-01 | アクション送信（attack） | ダメージ計算、HP 更新、ログ出力 |
| B-TURN-02 | アクション送信（heal） | HP 回復、ログ出力 |
| B-TURN-03 | ターン順序 | 素早さ順に処理 |
| B-TURN-04 | 死亡キャラにアクション | エラーまたはスキップ |
| B-CHEAT-01 | 無効なアクションタイプ | 拒否（サーバー権威モデル） |
| B-CHEAT-02 | 他プレイヤーのキャラを操作 | 拒否 |

#### 戦闘ログ（インラインタグ）

| # | テストケース | 期待結果 |
|---|------------|---------|
| B-LOG-01 | 攻撃ログ | `[e:angry][cam:shake]エミリの攻撃！[e:sad]スライムに10のダメージ！` 形式 |
| B-LOG-02 | 回復ログ | `[e:smile]エミリは回復薬を使った！[cam:flash]HPが30回復した！` 形式 |

### 4.4 TradeRoom テスト

```
ファイル: test/TradeRoom.test.ts
```

| # | テストケース | 期待結果 |
|---|------------|---------|
| T-OFFER-01 | アイテム出品 | state.offers に追加 |
| T-ACCEPT-01 | 取引承認 | 両者のインベントリ更新、DB 保存 |
| T-CANCEL-01 | 取引キャンセル | state.offers から削除 |
| T-INVALID-01 | 持っていないアイテムを出品 | 拒否 |
| T-INVALID-02 | 取引中に切断 | 自動キャンセル、ロールバック |

### 4.5 認証統合テスト（KaedevnAuthAdapter）

```
ファイル: test/KaedevnAuth.test.ts
```

| # | テストケース | 期待結果 |
|---|------------|---------|
| A-JWT-01 | kaedevn JWT を Colyseus onAuth で検証 | userId, role, status 取得 |
| A-JWT-02 | kaedevn のトークン構造（userId/role/status） | 正しくデコード |
| A-JWT-03 | トークンの secret 不一致 | 検証失敗 |
| A-JWT-04 | claims なしの古いトークン | DB ルックアップにフォールバック |
| A-GUEST-01 | guest ロールでのアクセス制限 | 取引・一部機能が制限される |
| A-CODE-01 | auth code → JWT 交換フロー | 30秒以内なら成功 |
| A-CODE-02 | auth code 期限切れ | 失敗 |
| A-CODE-03 | auth code 二重使用 | 2回目は失敗 |

### 4.6 インラインタグ統合テスト

```
ファイル: test/InlineTags.test.ts
```

| # | テストケース | 期待結果 |
|---|------------|---------|
| I-PARSE-01 | `"こんにちは。[e:smile]よろしく！"` | text + directive + text の3セグメント |
| I-PARSE-02 | `"[e:smile][p:wave][t]テスト"` | 3 directive + 1 text |
| I-PARSE-03 | `"[cam:zoom:1.2]ズーム"` | cam directive（value=zoom, param=1.2） |
| I-PARSE-04 | `"[click]次のページ"` | wait directive（value=click） |
| I-PARSE-05 | `"不正な[タグ"` | タグとして認識せず、テキストとして扱う |
| I-STRIP-01 | `stripTags("[e:smile]こんにちは[p:wave]")` | `"こんにちは"` |
| I-NPC-01 | NPC dialogue のタグがクライアントに届く | broadcast でタグ付きテキスト配信 |
| I-BATTLE-01 | 戦闘ログのタグがクライアントに届く | 同上 |

### 4.7 スケーリングテスト（Redis）

```
ファイル: test/Scaling.test.ts
前提: Redis 起動必須
```

| # | テストケース | 期待結果 |
|---|------------|---------|
| S-IPC-01 | サーバーA → サーバーB の Room 検索 | IPC 経由で Room 情報取得 |
| S-ZONE-01 | ゾーン移動（サーバー跨ぎ） | 旧サーバーで leave、新サーバーで join |
| S-CHAT-01 | グローバルチャット（サーバー跨ぎ） | Presence pub/sub で全サーバーに配信 |
| S-MATCH-01 | Redis Driver でのマッチメイキング | 全サーバーの Room を検索 |

---

## 5. テスト実行方法

### セットアップ

```bash
cd /Users/kentaromukunasi/Documents/git/colyseus

# 依存関係
pnpm install

# ビルド
npx tsx build.ts
cd packages/sdk && pnpm build && cd ../..
cd packages/monitor && pnpm build && cd ../..
cd packages/playground && pnpm build && cd ../..

# Redis（スケーリングテストに必要）
brew services start redis
```

### テスト実行

```bash
# 全テスト
cd bundles/colyseus && pnpm test

# 特定テスト
cd bundles/colyseus && npx mocha test/WorldRoom.test.ts --exit --timeout 15000

# Redis 不要テストのみ
cd bundles/colyseus && npx mocha test/WorldRoom.test.ts test/ChatRoom.test.ts test/BattleRoom.test.ts --exit --timeout 15000 --grep "LocalDriver"
```

### 環境変数

```bash
COLYSEUS_SEAT_RESERVATION_TIME=0.3    # テスト用短縮
COLYSEUS_PRESENCE_SHORT_TIMEOUT=300   # テスト用短縮
JWT_SECRET=test-secret-for-colyseus-mmo
```

---

## 6. 実装チェックリスト

次回セッションで以下の順に実装する:

### Step 1: 基盤（テスト先行）

- [ ] `test/mocks/kaedevn-auth.ts` — JWT モック作成
- [ ] `test/mocks/player-db.ts` — InMemoryPlayerDB 作成
- [ ] `test/mocks/zone-map.ts` — テストゾーンデータ作成
- [ ] `test/mocks/inline-tags.ts` — パーサーモック作成
- [ ] `src/schemas/PlayerState.ts` — @colyseus/schema 定義
- [ ] `src/schemas/WorldState.ts` — @colyseus/schema 定義

### Step 2: WorldRoom（TDD）

- [ ] `test/WorldRoom.test.ts` — テスト作成（W-AUTH-*, W-JOIN-*, W-MOVE-*, W-NPC-*)
- [ ] `src/rooms/WorldRoom.ts` — 実装
- [ ] `src/auth/KaedevnAuthAdapter.ts` — JWT 検証
- [ ] `src/persistence/PlayerPersistence.ts` — DB 永続化インターフェース

### Step 3: ChatRoom（TDD）

- [ ] `test/ChatRoom.test.ts` — テスト作成（C-*）
- [ ] `src/rooms/ChatRoom.ts` — 実装

### Step 4: BattleRoom（TDD）

- [ ] `test/BattleRoom.test.ts` — テスト作成（B-*）
- [ ] `src/rooms/BattleRoom.ts` — 実装
- [ ] `src/schemas/BattleState.ts` — @colyseus/schema 定義

### Step 5: 統合テスト

- [ ] `test/KaedevnAuth.test.ts` — 認証統合テスト
- [ ] `test/InlineTags.test.ts` — タグ統合テスト
- [ ] `test/Scaling.test.ts` — Redis スケーリングテスト

---

## 7. kaedevn 側の主要定数（テストで使用）

### 表情一覧（18種）

```
normal, smile, laugh, grin, wink, smirk,
sad, cry, shy,
angry, furious, serious,
surprised, shocked, blush,
sleepy, closed, cold
```

### ポーズ一覧（16種）

```
standing, relaxed, cool,
arms_crossed, hands_on_hips, wave, point, salute, think,
cheer, fighting, jump,
bow, shy_pose, tired, sit
```

### プリセット一覧

```
Female: hanako, misaki, sakura, shizue
Male:   taro, kenichi, shota, masao
```

### インラインタグ一覧

```
[e:名前]       — 表情変更
[p:名前]       — ポーズ変更
[t] [w] [s]    — リップシンク（talk/whisper/shout）
[cam:shake]    — 画面揺れ
[cam:zoom:倍率] — ズーム
[cam:flash]    — 白フラッシュ
[cam:dark]     — 暗転
[cam:reset]    — カメラリセット
[wait:ミリ秒]   — 一時停止
[click]        — クリック待ち
```

### kaedevn セーブスキーマ

```json
{
  "save_schema_version": 1,
  "engine_version": "",
  "work_id": "",
  "scenario_id": "",
  "node_id": "",
  "vars": {},
  "read": {},
  "timestamp": 0
}
```

### kaedevn API エンドポイント（MMO で使用するもの）

```
POST /api/auth/login        — JWT 取得
GET  /api/auth/me           — ユーザー情報
POST /api/auth/guest        — ゲストアカウント作成
POST /api/auth/code         — auth code 発行（30秒有効）
POST /api/auth/exchange     — code → JWT 交換
POST /api/cloud-saves       — セーブデータ保存
GET  /api/cloud-saves       — セーブデータ一覧
```

---

## メタ情報

| 項目 | 値 |
|------|-----|
| 生成モデル | Claude Opus 4.6 |
| 生成日 | 2026-03-28 |
| リポジトリ | colyseus + kaedevn-monorepo |
| バージョン | colyseus 0.17.x / kaedevn latest |
| 検証状態 | 両リポジトリのソースコード読解に基づく設計。未実装 |

> この文書は AI によって生成されました。内容の正確性はソースコードとの照合で確認してください。
