---
title: kaedevn テキスト型MMO 実装計画書
type: plan
project: colyseus + kaedevn
version: 0.17.x
date: 2026-03-28
author: Claude Opus 4.6
---

# kaedevn テキスト型MMO 実装計画書

> **Repository:** colyseus + kaedevn-monorepo
> **Generated by:** Claude Opus 4.6 | **Date:** 2026-03-28
> **Scope:** mmo/ ディレクトリの全実装

## 前提ドキュメント

実装前に以下を参照する:

| # | 文書 | 用途 |
|---|------|------|
| 14 | MMO設計書 | アーキテクチャ全体図、Room設計 |
| 16 | テスト・モック仕様書 | テストケース50+、モック4種のコード |
| 17 | フォーク開発戦略 | ディレクトリ構成、既存コード変更なしの方針 |
| 18 | DB設計書 | Prisma スキーマ、全テーブル定義 |
| 19 | メッセージプロトコル仕様 | Room別の全メッセージ型29種 |
| 20 | ゾーンマップ設計書 | ゾーン定義、NPC配置、敵出現 |

## 実装方針

- **TDD（テスト駆動開発）**: テストを先に書き、実装で通す
- **既存ファイル変更なし**: Colyseus 本体は触らない。`mmo/` に全部追加
- **各ステップの完了条件**: テストが全て passing であること

## 依存関係図

```
Step 0: プロジェクト初期化
  ↓
Step 1: モック + Schema
  ↓
Step 2: WorldRoom ←── Step 3: ChatRoom
  ↓                        ↓
Step 4: BattleRoom    Step 5: TradeRoom
  ↓                        ↓
Step 6: 統合テスト（Redis）
  ↓
Step 7: kaedevn クライアント統合
```

## Step 0: プロジェクト初期化

### やること

- `mmo/` ディレクトリ作成
- `package.json` 作成（colyseus, @colyseus/schema 依存）
- `tsconfig.json` 作成
- Mocha テスト設定

### 作成ファイル

```
mmo/
├── package.json
├── tsconfig.json
└── .mocharc.yml
```

### package.json の依存

```json
{
  "name": "@kaedevn/mmo",
  "private": true,
  "type": "module",
  "dependencies": {
    "colyseus": "workspace:*",
    "@colyseus/schema": "^4.0.0",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "@colyseus/testing": "workspace:*",
    "mocha": "^10.0.0",
    "@types/mocha": "^10.0.0",
    "tsx": "^4.0.0"
  }
}
```

### 完了条件

- [ ] `pnpm install` が通る
- [ ] 空のテストファイルが `pnpm test` で実行できる

### 規模

ファイル3個、30分

---

## Step 1: モック + Schema 定義

### やること

テスト仕様書（16番）のモック4種と Schema 定義を作成。

### 作成ファイル

```
mmo/
├── src/
│   ├── schemas/
│   │   ├── PlayerState.ts        ← @colyseus/schema
│   │   ├── WorldState.ts         ← @colyseus/schema (NPCState 含む)
│   │   └── BattleState.ts        ← @colyseus/schema (BattlerState 含む)
│   ├── auth/
│   │   └── KaedevnAuthAdapter.ts ← JWT 検証
│   ├── persistence/
│   │   └── PlayerPersistence.ts  ← interface + InMemory 実装
│   └── types/
│       └── messages.ts           ← メッセージプロトコル仕様（19番）の型定義
├── test/
│   └── mocks/
│       ├── kaedevn-auth.ts       ← JWT モック（16番からコピー）
│       ├── player-db.ts          ← InMemoryPlayerDB（16番からコピー）
│       ├── zone-map.ts           ← テストゾーンデータ（16番からコピー）
│       └── inline-tags.ts        ← パーサーモック（16番からコピー）
```

### 完了条件

- [ ] Schema が `@colyseus/schema` の `type()` デコレータでコンパイル通る
- [ ] KaedevnAuthAdapter でテストトークンの生成・検証ができる
- [ ] InMemoryPlayerDB で CRUD ができる
- [ ] parseInlineText で全タグがパースできる
- [ ] メッセージ型が TypeScript コンパイル通る

### 規模

ファイル10個、1-2時間

---

## Step 2: WorldRoom

### 依存

Step 1（モック + Schema）

### やること

テスト仕様書（16番）の W-* テストケースを実装。

### 作成ファイル

```
mmo/
├── src/rooms/
│   └── WorldRoom.ts
└── test/
    └── WorldRoom.test.ts
```

### テストケース（17件）

| カテゴリ | 件数 | ID |
|---------|------|-----|
| 認証 | 5 | W-AUTH-01〜05 |
| 参加・離脱 | 5 | W-JOIN-01〜03, W-LEAVE-01〜02, W-LEAVE-03(onDrop) |
| ゾーン移動 | 3 | W-MOVE-01〜03 |
| NPC | 3 | W-NPC-01〜03 |
| 同期 | 3 | W-SYNC-01〜03 |
| 永続化 | 2 | W-PERSIST-01〜02 |

### 実装順序

1. テスト: W-AUTH-* を書く → KaedevnAuthAdapter を Room.onAuth で使う実装
2. テスト: W-JOIN-* を書く → PlayerState 追加、DB ロードの実装
3. テスト: W-MOVE-* を書く → ゾーン移動メッセージの実装
4. テスト: W-NPC-* を書く → NPC インタラクションの実装
5. テスト: W-SYNC-* を書く → 表情・ポーズの状態同期
6. テスト: W-PERSIST-* を書く → onDispose での DB 保存

### 完了条件

- [ ] 17 テストケース全て passing
- [ ] 有効な JWT でジョインでき、無効な JWT で拒否される
- [ ] ゾーン移動メッセージが正しく送信される
- [ ] NPC のインラインタグ付きセリフが配信される

### 規模

ファイル2個、2-3時間

---

## Step 3: ChatRoom

### 依存

Step 1（モック + Schema）

### やること

テスト仕様書（16番）の C-* テストケースを実装。

### 作成ファイル

```
mmo/
├── src/rooms/
│   └── ChatRoom.ts
└── test/
    └── ChatRoom.test.ts
```

### テストケース（8件）

| ID | 内容 |
|-----|------|
| C-GLOBAL-01 | グローバルチャット送信 → 全員受信 |
| C-GLOBAL-02 | インラインタグ付きチャット |
| C-ZONE-01 | ゾーンチャット → 同一ゾーンのみ |
| C-WHISPER-01 | ウィスパー → ターゲットのみ |
| C-WHISPER-02 | 存在しないターゲット |
| C-RATE-01 | レート制限 |
| C-FORMAT-01 | 空メッセージ |
| C-FORMAT-02 | 長すぎるメッセージ |

### 完了条件

- [ ] 8 テストケース全て passing
- [ ] グローバル/ゾーン/ウィスパーの配信先が正しい

### 規模

ファイル2個、1時間

---

## Step 4: BattleRoom

### 依存

Step 2（WorldRoom — 戦闘開始のトリガーで WorldRoom から遷移）

### やること

テスト仕様書（16番）の B-* テストケースを実装。

### 作成ファイル

```
mmo/
├── src/rooms/
│   └── BattleRoom.ts
└── test/
    └── BattleRoom.test.ts
```

### テストケース（12件）

| カテゴリ | 件数 |
|---------|------|
| ライフサイクル | 5（B-CREATE-*, B-JOIN-01, B-END-*） |
| ターン制戦闘 | 5（B-TURN-*, B-CHEAT-*） |
| 戦闘ログ | 2（B-LOG-*） |

### 完了条件

- [ ] 12 テストケース全て passing
- [ ] ターン制戦闘が正しく処理される
- [ ] 不正アクション（チート）が拒否される
- [ ] 戦闘ログにインラインタグが含まれる

### 規模

ファイル2個、2-3時間

---

## Step 5: TradeRoom

### 依存

Step 2（WorldRoom — インベントリシステム）

### やること

テスト仕様書（16番）の T-* テストケースを実装。

### 作成ファイル

```
mmo/
├── src/rooms/
│   └── TradeRoom.ts
└── test/
    └── TradeRoom.test.ts
```

### テストケース（5件）

| ID | 内容 |
|-----|------|
| T-OFFER-01 | アイテム出品 |
| T-ACCEPT-01 | 取引承認 |
| T-CANCEL-01 | 取引キャンセル |
| T-INVALID-01 | 持っていないアイテム出品 |
| T-INVALID-02 | 取引中に切断 |

### 完了条件

- [ ] 5 テストケース全て passing
- [ ] インベントリの整合性が保たれる
- [ ] 切断時にロールバックされる

### 規模

ファイル2個、1-2時間

---

## Step 6: 統合テスト（Redis）

### 依存

Step 2〜5 全て

### やること

テスト仕様書（16番）の S-*, A-*, I-* テストケースを実装。

### 作成ファイル

```
mmo/test/
├── KaedevnAuth.test.ts     ← A-* (7件)
├── InlineTags.test.ts       ← I-* (8件)
└── Scaling.test.ts          ← S-* (4件、Redis 必須)
```

### テストケース（19件）

| カテゴリ | 件数 | Redis 必要 |
|---------|------|-----------|
| 認証統合 | 7 | いいえ |
| インラインタグ | 8 | いいえ |
| スケーリング | 4 | はい |

### 完了条件

- [ ] 19 テストケース全て passing（Redis 起動時）
- [ ] サーバー間のゾーン移動が動く
- [ ] グローバルチャットがサーバー跨ぎで動く

### 規模

ファイル3個、2時間

---

## Step 7: kaedevn クライアント統合

### 依存

Step 2〜6 全て + kaedevn-monorepo

### やること

kaedevn のエンジン（PixiJS + テキストウィンドウ + 図形キャラ）から Colyseus MMO サーバーに接続する。

### 変更ファイル（kaedevn-monorepo 側）

```
packages/web/src/
├── mmo/
│   ├── MMOClient.ts         ← @colyseus/sdk ラッパー
│   ├── MMORenderer.ts       ← 他プレイヤーの図形キャラ描画
│   └── MMOTextHandler.ts    ← インラインタグ付きメッセージ → TextWindow
└── mmo-demo.html            ← デモページ
```

### 完了条件

- [ ] ブラウザから WorldRoom に接続できる
- [ ] 他プレイヤーの図形キャラが表示される
- [ ] NPC のインラインタグ付きセリフが TextWindow に表示される
- [ ] チャットが送受信できる

### 規模

ファイル4個、3-4時間

---

## 全体サマリー

| Step | 内容 | テスト数 | 規模 | 依存 |
|------|------|---------|------|------|
| 0 | プロジェクト初期化 | 0 | 30分 | なし |
| 1 | モック + Schema | 0（ユニットテスト的に検証） | 1-2時間 | Step 0 |
| 2 | WorldRoom | 17 | 2-3時間 | Step 1 |
| 3 | ChatRoom | 8 | 1時間 | Step 1 |
| 4 | BattleRoom | 12 | 2-3時間 | Step 2 |
| 5 | TradeRoom | 5 | 1-2時間 | Step 2 |
| 6 | 統合テスト | 19 | 2時間 | Step 2-5 |
| 7 | クライアント統合 | — | 3-4時間 | Step 2-6 |
| **合計** | | **61** | **約13-18時間** | |

## セッション分割案

| セッション | Step | 目標 |
|-----------|------|------|
| セッション1 | 0 + 1 + 2 | WorldRoom まで動く（テスト17件 passing） |
| セッション2 | 3 + 4 | ChatRoom + BattleRoom（テスト20件追加） |
| セッション3 | 5 + 6 | TradeRoom + 統合テスト（テスト24件追加） |
| セッション4 | 7 | kaedevn クライアント統合（ブラウザで動く） |

---

## メタ情報

| 項目 | 値 |
|------|-----|
| 生成モデル | Claude Opus 4.6 |
| 生成日 | 2026-03-28 |
| リポジトリ | colyseus + kaedevn-monorepo |
| バージョン | 0.17.x |
| 検証状態 | 設計のみ・未実装 |

> この文書は AI によって生成されました。内容の正確性はソースコードとの照合で確認してください。
